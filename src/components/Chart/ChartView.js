import React from "react";

function ChartView() {
  return (
    <svg
      className="chart__svg"
      width={`${window.innerWidth}`}
      height="600"
    ></svg>
  );
}

export default ChartView;
