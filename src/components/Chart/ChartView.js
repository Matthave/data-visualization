import React from "react";

function ChartView() {
  return (
    <svg
      className="chart__svg"
      width={`${window.innerWidth}`}
      height="700"
    ></svg>
  );
}

export default ChartView;
